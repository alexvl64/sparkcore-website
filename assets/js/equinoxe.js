const T = {
  en: {
    htmlLang:'en',
    hdrTitle:'Equinoxe — Fund Factsheet',
    hdrDate:'March 1, 2026',
    hdrCta:'Schedule a Call',
    lblSpecs:'Product Specifications',
    lblLaunch:'Launch date',
    lblStrategy:'Strategy',
    valStrategy:'Non-directional / market neutral',
    lblStructure:'Structure',
    valStructure:'Regulated AIF (Estonia)',
    lblCurrency:'Reference currency',
    lblMinInvest:'Minimum investment',
    lblCurrencies:'Accepted currencies',
    lblLeverage:'Leverage',
    valLeverage:'No directional leverage',
    lblLiquidity:'Liquidity',
    valLiquidity:'Quarterly',
    lblLockup:'Minimum commitment',
    valLockup:'1 year',
    lblManager:'Fund manager',
    lblBrokers:'Brokers & custodians',
    lblLegal:'Legal counsel',
    lblAccounting:'Accounting',
    lblCompliance:'Compliance',
    lblFees:'Fees',
    lblFeeEntry:'Entry',
    valFeeEntry:'No entry fee.',
    lblFeeMgmt:'Management',
    valFeeMgmt:'Per year, quarterly.',
    lblFeePerf:'Performance',
    valFeePerf:'Quarterly, High-Water Mark.',
    lblFeeExit:'Exit',
    valFeeExit:'No exit before 1 year.',
    lblKeyFigs:'Key Figures*',
    lblKfReturn:'Annualised return',
    lblKfMaxdd:'Max drawdown',
    lblKfVol:'Volatility',
    lblKfRatios:'Sharpe / Sortino',
    lblTeam:'Team',
    lblRole1:'Manager · Trading & Strategy',
    valBio1:'20+ years trading, co-founder DSM. Technical analysis strategies on crypto-assets.',
    lblRole2:'Manager · Systems & Risk',
    valBio2:'Software engineer. Crypto investor since 2014. Founder Cointips, co-founder DSM.',
    lblRole3:'Management Board',
    lblStratTitle:'Strategy Overview',
    valStratIntro:'<strong>Equinoxe</strong> is the defensive pillar of the SparkCore range, a non-directional strategy dedicated to digital assets.<span class="sp-next">The fund aims to generate steady performance, decorrelated from major market trends, by relying on two distinct engines: relative selection between assets and the exploitation of price inefficiencies. Directional exposure is strictly controlled and remains marginal in the construction of the portfolio.</span><span class="sp-next">As the defensive pillar, Equinoxe is intended to provide a complementary, stabilising source of return alongside the more directional funds in the SparkCore range.</span>',
    lblOffensiveTag:'Relative-selection block — Long/Short',
    lblOffensiveName:'Long/Short Beta Neutral',
    valOffensiveDesc:'This block builds long and short positions calibrated so that performance derives from relative selection between assets, independently of the overall market direction. Alpha is sought by identifying digital assets likely to outperform or underperform their peers, within a rigorous framework controlling exposure, liquidity and position concentration.',
    lblDefensiveTag:'Arbitrage block — Relative Value',
    lblDefensiveName:'Relative Value / Arbitrage',
    valDefensiveDesc:'This block exploits price or valuation gaps between related instruments: spot and futures markets, funding rates, or highly correlated assets. These opportunities are captured without directional exposure, targeting identified and quantifiable inefficiency situations.',
    valStratBalance:'<strong>Overall strategic balance</strong> — Equinoxe combines these two complementary approaches within a unified risk framework. The allocation between the blocks is adjusted dynamically according to market conditions — liquidity, volatility, depth of opportunities — in order to optimise the risk/return trade-off in each configuration.<span class="sp-next">Residual directional exposure may occasionally exist, for execution reasons or temporary position adjustments, but it remains marginal and is not a performance driver.</span>',
    valStratRisk:'<strong>Risk profile</strong> — Equinoxe is designed for investors seeking defensive exposure to digital assets, with reduced sensitivity to the market\'s directional cycles. The fund targets rigorous risk control, with a maximum drawdown target of 5%.<span class="sp-next">Portfolio construction is organised around four control axes: directional exposure, the liquidity of the instruments used, position concentration, and portfolio resilience in market-stress conditions.</span>',
    lblChartTitle:'Performance Evolution — Base 100 · Linear Scale',
    legendBench:'Risk-free rate',
    valKfNote:'*All figures calculated since 01/01/2025 (target maximum drawdown 5%).',
    valChartNote:'<strong class="chart-note-lead">Real performance, net of fees.</strong> From January 2025 to the end of August 2025, returns were generated on managed accounts (real strategies, no backtest), outside the fund structure, prior to the fund\'s launch on 01/09/2025 (shaded area).',
    lblPerfTitle:'Monthly Performance History (%)',
    thYear:'Year',
    thJan:'Jan', thFeb:'Feb', thMar:'Mar', thApr:'Apr', thMay:'May', thJun:'Jun',
    thJul:'Jul', thAug:'Aug', thSep:'Sep', thOct:'Oct', thNov:'Nov', thDec:'Dec',
    thYtd:'YTD',
    valPerfNote:'Real performance, net of fees.',
    valDisclaimer:'<strong>Disclaimer:</strong> Past performance is neither a reliable indicator nor a guarantee of future performance. Crypto-assets carry a high level of risk including the risk of total loss of capital. This document is provided for informational purposes only and does not constitute an investment offer. The fund is marketed to professional investors. SparkCore.investment OÜ is registered and supervised by the Finantsinspektsioon (Estonia).',
    ftrCompany:'SparkCore.investment OÜ · Reg. No. 16265864',
    ftrAddress:'Männimäe 1, Pudisoo, 74626 Harju County, Estonia',
    ftrPrivacy:'Privacy Policy',
  },
  fr: {
    htmlLang:'fr',
    hdrTitle:'Equinoxe — Factsheet du fonds',
    hdrDate:'1er Mars 2026',
    hdrCta:'Planifier un appel',
    lblSpecs:'Spécification du produit',
    lblLaunch:'Date de lancement',
    lblStrategy:'Stratégie',
    valStrategy:'Non directionnelle / market neutral',
    lblStructure:'Structure',
    valStructure:'Fonds alternatif réglementé (FIA)',
    lblCurrency:'Devise de référence',
    lblMinInvest:'Capital minimum',
    lblCurrencies:'Devises acceptées',
    lblLeverage:'Effet de levier',
    valLeverage:'Aucun levier directionnel',
    lblLiquidity:'Liquidité',
    valLiquidity:'Trimestrielle',
    lblLockup:'Engagement minimum',
    valLockup:'1 an',
    lblManager:'Société de gestion',
    lblBrokers:'Courtiers & dépositaires',
    lblLegal:'Conseil juridique',
    lblAccounting:'Comptabilité',
    lblCompliance:'Compliance',
    lblFees:'Frais',
    lblFeeEntry:'Entrée',
    valFeeEntry:'Aucun frais.',
    lblFeeMgmt:'Gestion',
    valFeeMgmt:'Par an, trimestriel.',
    lblFeePerf:'Performance',
    valFeePerf:'Trimestriel, High-Water Mark.',
    lblFeeExit:'Sortie',
    valFeeExit:'Pas de sortie avant 1 an.',
    lblKeyFigs:'Chiffres clés*',
    lblKfReturn:'Rendement annualisé',
    lblKfMaxdd:'Perte maximale',
    lblKfVol:'Volatilité',
    lblKfRatios:'Sharpe / Sortino',
    lblTeam:'Équipe',
    lblRole1:'Gérant · Trading & Stratégie',
    valBio1:'+20 ans de trading, co-fondateur DSM. Stratégies basées sur l\'analyse technique des crypto-actifs.',
    lblRole2:'Gérant · Systèmes & risques',
    valBio2:'Ingénieur informatique. Investisseur crypto depuis 2014. Fondateur Cointips, co-fondateur DSM.',
    lblRole3:'Management Board',
    lblStratTitle:'Aperçu de la stratégie',
    valStratIntro:'<strong>Equinoxe</strong> est le pilier défensif de la gamme SparkCore, une stratégie non directionnelle dédiée aux actifs numériques.<span class="sp-next">Le fonds vise à générer une performance régulière et décorrélée des grandes tendances du marché, en s\'appuyant sur deux moteurs distincts : la sélection relative entre actifs et l\'exploitation d\'inefficiences de prix. L\'exposition directionnelle est strictement encadrée et reste marginale dans la construction du portefeuille.</span><span class="sp-next">En tant que pilier défensif, Equinoxe a vocation à apporter une source de rendement complémentaire et stabilisatrice face aux autres fonds plus directionnels de la gamme SparkCore.</span>',
    lblOffensiveTag:'Bloc sélection relative — Long/Short',
    lblOffensiveName:'Long/Short Beta Neutral',
    valOffensiveDesc:'Ce bloc construit des positions longues et courtes calibrées pour que la performance découle de la sélection relative entre actifs, indépendamment de la direction générale du marché. L\'alpha est recherché en identifiant les actifs numériques susceptibles de surperformer ou de sous-performer leurs pairs, dans un cadre rigoureux de contrôle de l\'exposition, de la liquidité et de la concentration des positions.',
    lblDefensiveTag:'Bloc arbitrage — Relative Value',
    lblDefensiveName:'Relative Value / Arbitrage',
    valDefensiveDesc:'Ce bloc exploite des écarts de prix ou de valorisation entre instruments liés : marchés spot et futures, taux de financement, ou actifs fortement corrélés. Ces opportunités sont captées sans exposition directionnelle, en ciblant des situations d\'inefficience identifiées et quantifiables.',
    valStratBalance:'<strong>Équilibre stratégique global</strong> — Equinoxe combine ces deux approches complémentaires dans un cadre de risque unifié. La répartition entre les blocs est ajustée dynamiquement en fonction des conditions de marché — liquidité, volatilité, profondeur des opportunités — afin d\'optimiser le couple rendement/risque à chaque configuration.<span class="sp-next">Une exposition directionnelle résiduelle peut exister ponctuellement, pour des raisons d\'exécution ou d\'ajustement temporaire des positions, mais elle reste marginale et ne constitue pas un moteur de performance.</span>',
    valStratRisk:'<strong>Profil de risque</strong> — Equinoxe s\'adresse aux investisseurs recherchant une exposition défensive aux actifs numériques, avec une sensibilité réduite aux cycles directionnels du marché. Le fonds vise une maîtrise rigoureuse du risque, avec une perte maximale ciblée de 5 %.<span class="sp-next">La construction du portefeuille s\'articule autour de quatre axes de contrôle : l\'exposition directionnelle, la liquidité des instruments utilisés, la concentration des positions, et la résilience du portefeuille en phase de stress de marché.</span>',
    lblChartTitle:'Évolution de la performance — base 100 · échelle linéaire',
    legendBench:'Taux sans risque',
    valKfNote:'*Indicateurs calculés depuis le 01/01/2025 (perte maximale cible 5%).',
    valChartNote:'<strong class="chart-note-lead">Performance réelle, nette de frais.</strong> De janvier 2025 à fin août 2025, les performances ont été réalisées sur comptes gérés (stratégies réelles, sans backtest), hors de la structure du fonds, avant le lancement du fonds le 01/09/2025 (zone grisée).',
    lblPerfTitle:'Historique mensuel des performances (%)',
    thYear:'Année',
    thJan:'Jan', thFeb:'Fév', thMar:'Mar', thApr:'Avr', thMay:'Mai', thJun:'Jun',
    thJul:'Jul', thAug:'Aoû', thSep:'Sep', thOct:'Oct', thNov:'Nov', thDec:'Déc',
    thYtd:'YTD',
    valPerfNote:'Performance réelle, nette de frais.',
    valDisclaimer:'<strong>Avertissement :</strong> Les performances passées ne constituent ni un indicateur fiable ni une garantie des performances futures. Les crypto-actifs présentent un risque élevé incluant un risque de perte totale du capital. Ce document est fourni à titre informatif uniquement et ne constitue pas une offre d\'investissement. Le fonds est commercialisé auprès d\'investisseurs professionnels. SparkCore.investment OÜ est enregistrée et supervisée par la Finantsinspektsioon (Estonie).',
    ftrCompany:'SparkCore.investment OÜ · N° d\'enregistrement 16265864',
    ftrAddress:'Männimäe 1, Pudisoo, 74626 Comté de Harju, Estonie',
    ftrPrivacy:'Politique de confidentialité',
  }
};

/* ── DATA SOURCES (Google Sheets) ── */
const FACTSHEET_SHEET_ID = "1GyKIEU3OS1QQVZ-Qm7EafRlZerNs3nZNHxuFFtkpQPk";
const GID_CHART_BASE100 = "1790044601";
const GID_FACTSHEET_SUMMARY = "1487010429";
const GID_MONTHLY_RETURNS = "880570107";

let summaryData = null;
let monthlyReturns = null; // Map(year -> Map(month -> percent number))

function gvizUrl(gid) {
  return `https://docs.google.com/spreadsheets/d/${FACTSHEET_SHEET_ID}/gviz/tq?tqx=out:json&gid=${gid}`;
}

async function fetchGvizJson(gid) {
  const r = await fetch(gvizUrl(gid));
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  const text = await r.text();
  return JSON.parse(text.substring(47).slice(0, -2));
}

function parseGvizDate(v) {
  if (!v) return null;
  const m = String(v).match(/^Date\((\d+),(\d+),(\d+)\)$/);
  if (!m) return null;
  return new Date(+m[1], +m[2], +m[3]);
}

function monthName(lang, monthIndex) {
  const en = ['January','February','March','April','May','June','July','August','September','October','November','December'];
  const fr = ['janvier','février','mars','avril','mai','juin','juillet','août','septembre','octobre','novembre','décembre'];
  const name = (lang === 'fr' ? fr : en)[monthIndex];
  return lang === 'fr' ? (name.charAt(0).toUpperCase() + name.slice(1)) : name;
}

function formatFactsheetDate(date, lang) {
  if (!(date instanceof Date)) return '';
  const day = date.getDate();
  const month = monthName(lang, date.getMonth());
  const year = date.getFullYear();
  if (lang === 'fr') {
    const dayStr = day === 1 ? '1er' : String(day);
    return `${dayStr} ${month} ${year}`;
  }
  return `${month} ${day}, ${year}`;
}

function setText(id, v) {
  const el = document.getElementById(id);
  if (el) el.textContent = v;
}

function updateHdrDateFromSummary() {
  if (!summaryData?.asOf) return;
  setText('hdr-date', formatFactsheetDate(summaryData.asOf, currentLang));
}

function setKeyFiguresFromSummary() {
  if (!summaryData) return;
  const pct = (x) => {
    if (typeof x !== 'number' || !isFinite(x)) return '—';
    const val = x * 100;
    const sign = val > 0 ? '+' : '';
    return `${sign}${val.toFixed(1)}%`;
  };
  const num = (x, d=2) => (typeof x === 'number' && isFinite(x)) ? x.toFixed(d) : '—';

  setText('kf-return', pct(summaryData.annualisedReturn));
  setText('kf-maxdd', pct(summaryData.maxDrawdown));
  setText('kf-vol', pct(summaryData.volatility));
  setText('kf-ratios', `${num(summaryData.sharpe, 2)} / ${num(summaryData.sortino, 2)}`);

  const setAria = (id, label) => {
    const el = document.getElementById(id);
    if (el) el.setAttribute('aria-label', label);
  };
  setAria('kf-return', `${T[currentLang].lblKfReturn}: ${pct(summaryData.annualisedReturn)}`);
  setAria('kf-maxdd', `${T[currentLang].lblKfMaxdd}: ${pct(summaryData.maxDrawdown)}`);
  setAria('kf-vol', `${T[currentLang].lblKfVol}: ${pct(summaryData.volatility)}`);
  setAria('kf-ratios', `${T[currentLang].lblKfRatios}: ${num(summaryData.sharpe,2)} / ${num(summaryData.sortino,2)}`);
}

function updateJsonLdDateModifiedFromSummary() {
  if (!summaryData?.asOf) return;
  const iso = summaryData.asOf.toISOString().slice(0, 10);
  const scripts = Array.from(document.querySelectorAll('script[type="application/ld+json"]'));
  for (const s of scripts) {
    try {
      const obj = JSON.parse(s.textContent);
      if (obj && Array.isArray(obj['@graph'])) {
        for (const node of obj['@graph']) {
          if (node && node['@type'] === 'WebPage' && typeof node.dateModified === 'string') {
            node.dateModified = iso;
          }
        }
        s.textContent = JSON.stringify(obj, null, 2);
      }
    } catch(e) {}
  }
}

function buildMonthlyTable() {
  if (!monthlyReturns) return;
  const body = document.getElementById('perf-body');
  if (!body) return;

  const years = Array.from(monthlyReturns.keys()).sort((a,b)=>a-b);
  const fmt = (x) => (typeof x === 'number' && isFinite(x)) ? x.toFixed(1) : '—';
  const classFor = (x) => (typeof x === 'number' && isFinite(x)) ? (x >= 0 ? 'pos' : 'neg') : '';
  const ytdCompoundPct = (vals) => {
    let acc = 1.0;
    let seen = false;
    for (const v of vals) {
      if (typeof v !== 'number' || !isFinite(v)) continue;
      seen = true;
      acc *= (1 + v/100);
    }
    return seen ? (acc - 1) * 100 : null;
  };

  body.innerHTML = '';
  for (const y of years) {
    const tr = document.createElement('tr');
    const th = document.createElement('th');
    th.setAttribute('scope', 'row');
    th.textContent = String(y);
    tr.appendChild(th);

    const vals = [];
    for (let m=1;m<=12;m++) {
      const v = monthlyReturns.get(y)?.get(m);
      vals.push(v);
      const td = document.createElement('td');
      if (typeof v === 'number' && isFinite(v)) td.className = classFor(v);
      td.textContent = fmt(v);
      tr.appendChild(td);
    }

    const ytd = ytdCompoundPct(vals);
    const tdYtd = document.createElement('td');
    tdYtd.className = 'ytd';
    tdYtd.textContent = (typeof ytd === 'number' && isFinite(ytd)) ? ytd.toFixed(1) : '—';
    tr.appendChild(tdYtd);

    body.appendChild(tr);
  }
}

function applyLang(lang) {
  const t = T[lang];
  document.documentElement.lang = t.htmlLang;
  const metaDesc = document.querySelector('meta[name="description"]');
  if (metaDesc) {
    metaDesc.content = lang === 'fr'
      ? 'Equinoxe est le pilier défensif de la gamme SparkCore — fonds alternatif crypto réglementé (FIA Estonie). Stratégie non directionnelle, market neutral et arbitrage, liquidité trimestrielle, minimum 50 000 €. Supervisé par la Finantsinspektsioon.'
      : 'Equinoxe is the defensive pillar of the SparkCore range — a regulated crypto-asset alternative investment fund (AIF Estonia). Non-directional, market-neutral and arbitrage strategy, quarterly liquidity, 50 000 € minimum. Supervised by Finantsinspektsioon.';
  }
  document.title = lang === 'fr'
    ? 'Equinoxe – Factsheet Fonds Crypto | SparkCore Fund Management'
    : 'Equinoxe – Crypto Fund Factsheet | SparkCore Fund Management';

  const s = (id, v) => { const el = document.getElementById(id); if(el) el.textContent = v; };
  const h = (id, v) => { const el = document.getElementById(id); if(el) el.innerHTML = v; };
  s('hdr-title', t.hdrTitle); s('hdr-date', t.hdrDate); s('hdr-cta', t.hdrCta);
  const toCamel = k => k.replace(/-([a-z])/g, (_, c) => c.toUpperCase());
  document.querySelectorAll('[data-seo-id]').forEach(el => {
    const key = toCamel(el.getAttribute('data-seo-id'));
    if (t[key]) el.textContent = t[key];
  });
  s('lbl-launch', t.lblLaunch); s('lbl-strategy', t.lblStrategy);
  s('val-strategy', t.valStrategy); s('lbl-structure', t.lblStructure); s('val-structure', t.valStructure);
  s('lbl-currency', t.lblCurrency); s('lbl-mininvest', t.lblMinInvest); s('lbl-currencies', t.lblCurrencies);
  s('lbl-leverage', t.lblLeverage); s('val-leverage', t.valLeverage); s('lbl-liquidity', t.lblLiquidity);
  s('val-liquidity', t.valLiquidity); s('lbl-lockup', t.lblLockup); s('val-lockup', t.valLockup);
  s('lbl-manager', t.lblManager); s('lbl-brokers', t.lblBrokers); s('lbl-legal', t.lblLegal);
  s('lbl-accounting', t.lblAccounting); s('lbl-compliance', t.lblCompliance);
  s('lbl-fee-entry', t.lblFeeEntry); s('val-fee-entry', t.valFeeEntry);
  s('lbl-fee-mgmt', t.lblFeeMgmt); s('val-fee-mgmt', t.valFeeMgmt); s('lbl-fee-perf', t.lblFeePerf);
  s('val-fee-perf', t.valFeePerf); s('lbl-fee-exit', t.lblFeeExit); s('val-fee-exit', t.valFeeExit);
  s('lbl-kf-return', t.lblKfReturn); s('lbl-kf-maxdd', t.lblKfMaxdd);
  s('lbl-kf-vol', t.lblKfVol); s('lbl-kf-ratios', t.lblKfRatios);
  s('lbl-role-1', t.lblRole1); s('val-bio-1', t.valBio1);
  s('lbl-role-2', t.lblRole2); s('val-bio-2', t.valBio2); s('lbl-role-3', t.lblRole3);
  h('val-strat-intro', t.valStratIntro);
  s('lbl-offensive-tag', t.lblOffensiveTag); s('lbl-offensive-name', t.lblOffensiveName);
  s('val-offensive-desc', t.valOffensiveDesc); s('lbl-defensive-tag', t.lblDefensiveTag);
  s('lbl-defensive-name', t.lblDefensiveName); s('val-defensive-desc', t.valDefensiveDesc);
  h('val-strat-balance', t.valStratBalance); h('val-strat-risk', t.valStratRisk);
  s('val-kf-note', t.valKfNote);
  h('val-chart-note', t.valChartNote);
  s('th-year', t.thYear);
  s('th-jan', t.thJan); s('th-feb', t.thFeb); s('th-mar', t.thMar); s('th-apr', t.thApr); s('th-may', t.thMay); s('th-jun', t.thJun);
  s('th-jul', t.thJul); s('th-aug', t.thAug); s('th-sep', t.thSep); s('th-oct', t.thOct); s('th-nov', t.thNov); s('th-dec', t.thDec);
  s('th-ytd', t.thYtd);
  s('val-perf-note', t.valPerfNote); h('val-disclaimer', t.valDisclaimer);
  s('ftr-company', t.ftrCompany); s('ftr-address', t.ftrAddress); s('ftr-privacy', t.ftrPrivacy);
  document.getElementById('btn-en').classList.toggle('active', lang==='en');
  document.getElementById('btn-en').setAttribute('aria-pressed', lang==='en');
  document.getElementById('btn-fr').classList.toggle('active', lang==='fr');
  document.getElementById('btn-fr').setAttribute('aria-pressed', lang==='fr');

  // Dynamic values that depend on current language formatting
  updateHdrDateFromSummary();
  setKeyFiguresFromSummary();
}

let currentLang = 'en';
function setLang(lang) {
  currentLang = lang;
  try { localStorage.setItem('sc_factsheet_lang', lang); } catch(e){}
  applyLang(lang);
}

(function initLang() {
  let saved = null;
  try { saved = localStorage.getItem('sc_factsheet_lang'); } catch(e){}
  const browser = (navigator.language || navigator.userLanguage || '').toLowerCase();
  const detected = saved || (browser.startsWith('fr') ? 'fr' : 'en');
  currentLang = detected;
  applyLang(detected);
})();

/* ── CHART (identique à l'original) ── */
const SHEET_ID = FACTSHEET_SHEET_ID;
const SHEET_GID = GID_CHART_BASE100;
const dynamicTrendsValue = [], btcBase100 = [];
// Managed accounts (real strategies, outside the fund) until the launch on
// 01/09/2025: shaded on the chart so the two periods are never read as one.
const EQ_FUND_LAUNCH = '2025-09-01';
function utcTs(iso) { const [y, m, d] = iso.split('-').map(Number); return Date.UTC(y, m - 1, d); }

// Données chart INVENTÉES (placeholder Equinoxe — base 100). [year, monthIndex0, equinoxe, cci30]
const EQX_CHART = [
  [2026,0,100.0,100.0],[2026,1,100.8,92.0],[2026,2,101.5,108.0],[2026,3,102.1,95.0],
  [2026,4,102.9,112.0],[2026,5,103.4,104.0],[2026,6,104.2,118.0],[2026,7,104.9,99.0],
  [2026,8,105.6,90.0],[2026,9,106.1,107.0],[2026,10,106.9,121.0],[2026,11,107.6,113.0],
];
// Unified loader: real Equinoxe data from the SparkCore-published JSON (R2, via
// the Pages Function) replaces the former invented placeholder (EQX_CHART + the
// hardcoded summary/monthly below, now unused). Units: summary fractions,
// monthly percent, chart base 100 — render functions unchanged.
async function loadFactsheet() {
  try {
    const r = await fetch('/data/funds/equinoxe.json', { cache: 'no-cache' });
    if (!r.ok) throw new Error(`HTTP ${r.status}`);
    const d = await r.json();

    summaryData = {
      asOf: d.as_of ? new Date(d.as_of + 'T00:00:00') : null,
      annualisedReturn: d.summary?.annualised_return ?? null,
      maxDrawdown: d.summary?.max_drawdown ?? null,
      volatility: d.summary?.volatility ?? null,
      sharpe: d.summary?.sharpe ?? null,
      sortino: d.summary?.sortino ?? null,
    };
    updateHdrDateFromSummary();
    setKeyFiguresFromSummary();
    updateJsonLdDateModifiedFromSummary();

    monthlyReturns = new Map();
    for (const m of (d.monthly_returns || [])) {
      if (!monthlyReturns.has(m.year)) monthlyReturns.set(m.year, new Map());
      monthlyReturns.get(m.year).set(m.month, m.return);
    }
    buildMonthlyTable();

    dynamicTrendsValue.length = 0; btcBase100.length = 0;
    for (const p of (d.chart_base100 || [])) {
      const x = utcTs(p.date);
      dynamicTrendsValue.push({ x, y: p.fund });
      btcBase100.push({ x, y: p.benchmark });
    }
    renderChart();
  } catch (e) {
    console.error(e);
    const c = document.getElementById('data-container');
    if (c) c.textContent = currentLang === 'fr' ? 'Impossible de charger les données.' : 'Unable to load data.';
  }
}

function renderChart() {
  const draw = () => new ApexCharts(document.querySelector('#chart-el'), {
    series: [{name:'Equinoxe',data:dynamicTrendsValue},{name:(currentLang==='fr'?'Taux sans risque':'Risk-free rate'),data:btcBase100}],
    chart: {height:260,type:'line',zoom:{enabled:false},toolbar:{show:false},animations:{enabled:false},background:'transparent',fontFamily:'Inter, sans-serif'},
    stroke: {width:[2,1.5],dashArray:[0,6]},
    colors: ['#111111','#8A8E96'],
    annotations: {xaxis: [
      {x: dynamicTrendsValue[0].x, x2: utcTs(EQ_FUND_LAUNCH), fillColor: '#8A8E96', opacity: 0.12, borderColor: 'transparent',
       label: {text: currentLang === 'fr' ? 'Comptes gérés' : 'Managed accounts', orientation: 'horizontal', position: 'top', textAnchor: 'start', offsetX: 4, offsetY: 4, borderWidth: 0, style: {background: 'transparent', color: '#1B2D4D', fontSize: '9px', fontFamily: 'Inter, sans-serif'}}},
      {x: utcTs(EQ_FUND_LAUNCH), borderColor: '#4C5B7A', strokeDashArray: 3,
       label: {text: currentLang === 'fr' ? 'Lancement du fonds' : 'Fund launch', orientation: 'horizontal', position: 'top', textAnchor: 'start', offsetX: 4, offsetY: 4, borderWidth: 0, style: {background: 'transparent', color: '#4C5B7A', fontSize: '9px', fontFamily: 'Inter, sans-serif'}}},
    ]},
    xaxis: {type:'datetime',axisTicks:{show:false},axisBorder:{show:false},tooltip:{enabled:false},labels:{hideOverlappingLabels:true,rotate:0,formatter:(v, ts) => new Date(ts != null ? ts : v).toLocaleDateString(currentLang === 'fr' ? 'fr-FR' : 'en-US', {month:'short', year:'numeric', timeZone:'UTC'}),style:{colors:'#1B2D4D',fontFamily:'Inter, sans-serif',fontSize:'9px'}}},
    yaxis: {logarithmic:false,labels:{show:false},axisTicks:{show:false},axisBorder:{show:false}},
    grid: {yaxis:{lines:{show:false}},padding:{left:30,right:14,top:-10,bottom:0}},
    legend: {show:false},
    tooltip: {theme:'light',style:{fontFamily:'Inter, sans-serif',fontSize:'11px'},x:{formatter:ts => new Date(ts).toLocaleDateString(currentLang === 'fr' ? 'fr-FR' : 'en-US', {day:'numeric', month:'long', year:'numeric', timeZone:'UTC'})}},
  }).render();
  if (document.fonts && document.fonts.ready) { document.fonts.ready.then(draw); } else { draw(); }
}

loadFactsheet();

/* ── Cal.eu Popup Embed ── */
(function (C, A, L) {
  let p = function (a, ar) { a.q.push(ar); };
  let d = C.document;
  C.Cal = C.Cal || function () {
    let cal = C.Cal;
    let ar = arguments;
    if (!cal.loaded) {
      cal.ns = {};
      cal.q = cal.q || [];
      d.head.appendChild(d.createElement("script")).src = A + "/embed/embed.js";
      cal.loaded = true;
    }
    if (ar[0] === L) {
      const api = function () { p(api, arguments); };
      const namespace = ar[1];
      api.q = api.q || [];
      if (typeof namespace === "string") {
        cal.ns[namespace] = cal.ns[namespace] || api;
        p(cal.ns[namespace], ar);
        p(cal, ["initNamespace", namespace]);
      } else p(cal, ar);
      return;
    }
    p(cal, ar);
  };
})(window, "https://app.cal.eu", "init");

Cal("init", { origin: "https://app.cal.eu" });
Cal("ui", {
  theme: "light",
  cssVarsPerTheme: {
    light: { "cal-brand": "#0E1117" }
  }
});

/* ── SUMMARY + MONTHLY RETURNS (dynamic blocks) ── */
function fetchFactsheetSummary() {
  // Key figures INVENTÉES (placeholder Equinoxe — profil défensif, drawdown cible 8%)
  summaryData = {
    asOf: new Date(2026, 4, 31),
    annualisedReturn: 0.092,
    maxDrawdown: -0.078,
    volatility: 0.061,
    sharpe: 1.45,
    sortino: 2.10,
  };
  updateHdrDateFromSummary();
  setKeyFiguresFromSummary();
  updateJsonLdDateModifiedFromSummary();
}

function fetchMonthlyReturns() {
  // Perf mensuelle INVENTÉE (placeholder Equinoxe, %)
  monthlyReturns = new Map();
  const rows = [
    [2026,1,0.8],[2026,2,0.7],[2026,3,0.6],[2026,4,0.8],[2026,5,0.5],[2026,6,0.8],
    [2026,7,0.7],[2026,8,0.7],[2026,9,0.5],[2026,10,0.8],[2026,11,0.7],[2026,12,0.7],
  ];
  for (const [y,m,v] of rows) {
    if (!monthlyReturns.has(y)) monthlyReturns.set(y, new Map());
    monthlyReturns.get(y).set(m, v);
  }
  buildMonthlyTable();
}

/* SUMMARY + MONTHLY + CHART are now loaded together from R2 by loadFactsheet()
   (see the CHART section). The placeholder data above is kept but unused. */
