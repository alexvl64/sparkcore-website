const T = {
  en: {
    htmlLang:'en',
    hdrTitle:'CryptoVision — Fund Factsheet',
    hdrDate:'March 1, 2026',
    hdrCta:'Schedule a Call',
    lblSpecs:'Product Specifications',
    lblLaunch:'Launch date',
    lblStrategy:'Strategy',
    valStrategy:'Discretionary & systematic',
    lblStructure:'Structure',
    valStructure:'Regulated AIF (Estonia)',
    lblCurrency:'Reference currency',
    lblMinInvest:'Minimum investment',
    lblCurrencies:'Accepted currencies',
    lblLeverage:'Leverage',
    valLeverage:'Yes (x2 max)',
    lblLiquidity:'Liquidity',
    valLiquidity:'Quarterly',
    lblLockup:'Minimum commitment',
    valLockup:'1 year',
    lblManager:'Fund manager',
    lblBrokers:'Brokers & custodians',
    lblLegal:'Legal counsel',
    lblAccounting:'Accounting',
    lblCompliance:'Compliance',
    lblFees:'Fees',
    lblFeeEntry:'Entry',
    valFeeEntry:'No entry fee.',
    lblFeeMgmt:'Management',
    valFeeMgmt:'Per year, quarterly.',
    lblFeePerf:'Performance',
    valFeePerf:'High-Water Mark.',
    lblFeeExit:'Exit',
    valFeeExit:'*4% before 1 year.',
    lblKeyFigs:'Key Figures*',
    lblKfReturn:'Annualised return',
    lblKfMaxdd:'Max drawdown',
    lblKfVol:'Volatility',
    lblKfRatios:'Sharpe / Sortino',
    valKfNote:'*All figures calculated since 13/03/2025. Since the March 2025 strategy update (target maximum drawdown 15%).',
    lblTeam:'Team',
    lblRole1:'Manager · Trading & Strategy',
    valBio1:'20+ years trading, co-founder DSM. Technical analysis strategies on crypto-assets.',
    lblRole2:'Manager · Systems & Risk',
    valBio2:'Software engineer. Crypto investor since 2014. Founder Cointips, co-founder DSM.',
    lblRole3:'Management Board',
    lblStratTitle:'Strategy Overview',
    valStratIntro:'<strong>CryptoVision</strong> is SparkCore\'s active, defensive strategy. The fund combines an actively managed directional sleeve with a market-neutral core to participate in the potential of digital assets without taking permanent passive exposure.<span class="sp-next">Since its strategy overhaul in March 2025, CryptoVision targets more durable and resilient exposure to digital assets, with a maximum drawdown target of 15%. The allocation between the two blocks is adjusted dynamically according to market conditions, volatility, liquidity and the risk/return profile. The portfolio is rebalanced monthly within a strict risk-management framework.</span>',
    lblDefensiveTag:'Defensive block — Primary core',
    lblDefensiveName:'Market-neutral & defensive algorithms',
    valDefensiveDesc:'This block brings together market-neutral strategies and defensive algorithmic approaches. Its role is to ensure the structural stability of the portfolio, including during periods of high volatility or market stress. Active management of exposure, liquidity and drawdown is central, with algorithmic execution governed by strict risk parameters.',
    lblOffensiveTag:'Offensive block — Opportunistic sleeve',
    lblOffensiveName:'Managed directional exposure',
    valOffensiveDesc:'This sleeve actively invests in the most liquid digital assets, primarily Bitcoin and Ethereum. It aims to capture favourable market phases when trend, momentum and risk signals justify it. Exposure may be reduced when visibility decreases, volatility rises or the risk/return profile becomes less favourable. Short positions may be initiated on reversal signals, and moderate leverage may be used within a defined risk framework.',
    valStratBalance:'<strong>Overall strategic balance</strong> — CryptoVision is built on a flexible allocation between a primary defensive core and a managed directional sleeve.<span class="sp-next">The defensive block seeks to stabilise the fund\'s profile, while the offensive block enables participation in favourable market phases. The weighting between the blocks evolves over time to preserve consistency between targeted performance, accepted volatility and drawdown control.</span>',
    valStratRisk:'<strong>Risk profile</strong> — CryptoVision is designed for investors seeking active, structured and defensive exposure to digital assets.<span class="sp-next">The fund positions itself between an offensive directional strategy and a market-neutral strategy. It accepts a degree of directional exposure, but this remains managed within a defined risk framework, with particular attention paid to volatility, the liquidity of the instruments used and the portfolio\'s behaviour in market-stress conditions.</span>',
    lblChartTitle:'Performance Evolution — Base 100 · Linear Scale',
    valChartNote:'Fund launched 13/03/2025. Gross of fees. Since March 2025, the strategy has been updated to integrate a strengthened defensive algorithmic core.',
    lblPerfTitle:'Monthly Performance History (%)',
    thYear:'Year',
    thJan:'Jan', thFeb:'Feb', thMar:'Mar', thApr:'Apr', thMay:'May', thJun:'Jun',
    thJul:'Jul', thAug:'Aug', thSep:'Sep', thOct:'Oct', thNov:'Nov', thDec:'Dec',
    thYtd:'YTD',
    valPerfNote:'Past performance does not guarantee future results. Gross of fees.',
    valDisclaimer:'<strong>Disclaimer:</strong> Past performance is neither a reliable indicator nor a guarantee of future performance. Crypto-assets carry a high level of risk including the risk of total loss of capital. This document is provided for informational purposes only and does not constitute an investment offer. The strategy is reserved for qualified or professional investors. SparkCore.investment OÜ is registered and supervised by the Finantsinspektsioon (Estonia).',
    ftrCompany:'SparkCore.investment OÜ · Reg. No. 16265864',
    ftrAddress:'Männimäe 1, Pudisoo, 74626 Harju County, Estonia',
    ftrPrivacy:'Privacy Policy',
  },
  fr: {
    htmlLang:'fr',
    hdrTitle:'CryptoVision — Factsheet du fonds',
    hdrDate:'1er Mars 2026',
    hdrCta:'Planifier un appel',
    lblSpecs:'Spécification du produit',
    lblLaunch:'Date de lancement',
    lblStrategy:'Stratégie',
    valStrategy:'Discrétionnaire & systématique',
    lblStructure:'Structure',
    valStructure:'Fonds alternatif réglementé (FIA)',
    lblCurrency:'Devise de référence',
    lblMinInvest:'Capital minimum',
    lblCurrencies:'Devises acceptées',
    lblLeverage:'Effet de levier',
    valLeverage:'Oui (x2 maximum)',
    lblLiquidity:'Liquidité',
    valLiquidity:'Trimestrielle',
    lblLockup:'Engagement minimum',
    valLockup:'1 an',
    lblManager:'Société de gestion',
    lblBrokers:'Courtiers & dépositaires',
    lblLegal:'Conseil juridique',
    lblAccounting:'Comptabilité',
    lblCompliance:'Compliance',
    lblFees:'Frais',
    lblFeeEntry:'Entrée',
    valFeeEntry:'Aucun frais.',
    lblFeeMgmt:'Gestion',
    valFeeMgmt:'Par an, trimestriel.',
    lblFeePerf:'Performance',
    valFeePerf:'High-Water Mark.',
    lblFeeExit:'Sortie',
    valFeeExit:'*4% avant 1 an.',
    lblKeyFigs:'Chiffres clés*',
    lblKfReturn:'Rendement annualisé',
    lblKfMaxdd:'Perte maximale',
    lblKfVol:'Volatilité',
    lblKfRatios:'Sharpe / Sortino',
    valKfNote:'*Indicateurs calculés depuis le 13/03/2025. Depuis la mise à jour de stratégie de mars 2025 (perte maximale cible 15%).',
    lblTeam:'Équipe',
    lblRole1:'Gérant · Trading & Stratégie',
    valBio1:'+20 ans de trading, co-fondateur DSM. Stratégies basées sur l\'analyse technique des crypto-actifs.',
    lblRole2:'Gérant · Systèmes & risques',
    valBio2:'Ingénieur informatique. Investisseur crypto depuis 2014. Fondateur Cointips, co-fondateur DSM.',
    lblRole3:'Management Board',
    lblStratTitle:'Aperçu de la stratégie',
    valStratIntro:'<strong>CryptoVision</strong> est la stratégie active et défensive de SparkCore. Le fonds combine une poche directionnelle pilotée et un socle market neutral afin de participer au potentiel des actifs numériques sans adopter une exposition passive permanente.<span class="sp-next">Depuis la refonte de sa stratégie en mars 2025, CryptoVision vise une exposition plus durable et résiliente aux actifs numériques, avec une perte maximale ciblée de 15 %. L\'allocation entre les deux blocs est ajustée dynamiquement selon les conditions de marché, la volatilité, la liquidité et le rapport rendement/risque. Le portefeuille fait l\'objet de rééquilibrages mensuels dans un cadre de gestion du risque strict.</span>',
    lblDefensiveTag:'Bloc défensif — Socle principal',
    lblDefensiveName:'Market neutral & algorithmique défensif',
    valDefensiveDesc:'Ce bloc regroupe des stratégies neutres au marché et des approches algorithmiques défensives. Son rôle est d\'assurer la stabilité structurelle du portefeuille, y compris lors des phases de forte volatilité ou de stress de marché. Le pilotage de l\'exposition, de la liquidité et de la perte maximale est central, avec une exécution algorithmique encadrée par des paramètres de risque stricts.',
    lblOffensiveTag:'Bloc offensif — Poche d\'opportunité',
    lblOffensiveName:'Exposition directionnelle pilotée',
    valOffensiveDesc:'Cette poche investit activement sur les actifs numériques les plus liquides, principalement Bitcoin et Ethereum. Elle vise à capter les phases de marché favorables lorsque les signaux de tendance, de momentum et de risque le justifient. L\'exposition peut être réduite lorsque la visibilité diminue, que la volatilité augmente ou que le rapport rendement/risque devient moins favorable. Des positions vendeuses peuvent être initiées en cas de signaux de retournement, et un effet de levier modéré peut être utilisé dans un cadre de risque défini.',
    valStratBalance:'<strong>Équilibre stratégique global</strong> — CryptoVision repose sur une allocation flexible entre un socle défensif principal et une poche directionnelle pilotée.<span class="sp-next">Le bloc défensif cherche à stabiliser le profil du fonds, tandis que le bloc offensif permet de participer aux phases favorables du marché. La pondération entre les blocs évolue dans le temps afin de préserver la cohérence entre performance recherchée, volatilité acceptée et maîtrise de la perte maximale.</span>',
    valStratRisk:'<strong>Profil de risque</strong> — CryptoVision s\'adresse aux investisseurs recherchant une exposition active, structurée et défensive aux actifs numériques.<span class="sp-next">Le fonds se positionne entre une stratégie directionnelle offensive et une stratégie neutre au marché. Il accepte une part d\'exposition directionnelle, mais celle-ci reste pilotée dans un cadre de risque défini, avec une attention particulière portée à la volatilité, à la liquidité des instruments utilisés et au comportement du portefeuille en phase de stress de marché.</span>',
    lblChartTitle:'Évolution de la performance — base 100 · échelle linéaire',
    valChartNote:'Lancement du fonds le 13 mars 2025. Brut de frais. Depuis mars 2025, la stratégie a été modifiée pour intégrer un socle algorithmique défensif renforcé.',
    lblPerfTitle:'Historique mensuel des performances (%)',
    thYear:'Année',
    thJan:'Jan', thFeb:'Fév', thMar:'Mar', thApr:'Avr', thMay:'Mai', thJun:'Jun',
    thJul:'Jul', thAug:'Aoû', thSep:'Sep', thOct:'Oct', thNov:'Nov', thDec:'Déc',
    thYtd:'YTD',
    valPerfNote:'Les performances passées ne garantissent pas les performances futures. Brut de frais.',
    valDisclaimer:'<strong>Avertissement :</strong> Les performances passées ne constituent ni un indicateur fiable ni une garantie des performances futures. Les crypto-actifs présentent un risque élevé incluant un risque de perte totale du capital. Ce document est fourni à titre informatif uniquement et ne constitue pas une offre d\'investissement. La stratégie est réservée aux investisseurs qualifiés ou professionnels. SparkCore.investment OÜ est enregistrée et supervisée par la Finantsinspektsioon (Estonie).',
    ftrCompany:'SparkCore.investment OÜ · N° d\'enregistrement 16265864',
    ftrAddress:'Männimäe 1, Pudisoo, 74626 Comté de Harju, Estonie',
    ftrPrivacy:'Politique de confidentialité',
  }
};

/* ── DATA SOURCES (Google Sheets) ── */
// CryptoVision factsheet: same structure as Dynamic Trends but different Sheet ID
const FACTSHEET_SHEET_ID = "1mH2HtAtqzLJNYMI5gdm0EX6u4E8hkvwH5LPVXXfwpnk";
const GID_CHART_BASE100 = "1790044601";
const GID_FACTSHEET_SUMMARY = "1487010429";
const GID_MONTHLY_RETURNS = "880570107";

let summaryData = null;
let monthlyReturns = null; // Map(year -> Map(month -> percent number))

function gvizUrl(gid) {
  return `https://docs.google.com/spreadsheets/d/${FACTSHEET_SHEET_ID}/gviz/tq?tqx=out:json&gid=${gid}`;
}

async function fetchGvizJson(gid) {
  const r = await fetch(gvizUrl(gid));
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  const text = await r.text();
  return JSON.parse(text.substring(47).slice(0, -2));
}

function parseGvizDate(v) {
  if (!v) return null;
  const m = String(v).match(/^Date\((\d+),(\d+),(\d+)\)$/);
  if (!m) return null;
  return new Date(+m[1], +m[2], +m[3]);
}

function monthName(lang, monthIndex) {
  const en = ['January','February','March','April','May','June','July','August','September','October','November','December'];
  const fr = ['janvier','février','mars','avril','mai','juin','juillet','août','septembre','octobre','novembre','décembre'];
  const name = (lang === 'fr' ? fr : en)[monthIndex];
  return lang === 'fr' ? (name.charAt(0).toUpperCase() + name.slice(1)) : name;
}

function formatFactsheetDate(date, lang) {
  if (!(date instanceof Date)) return '';
  const day = date.getDate();
  const month = monthName(lang, date.getMonth());
  const year = date.getFullYear();
  if (lang === 'fr') {
    const dayStr = day === 1 ? '1er' : String(day);
    return `${dayStr} ${month} ${year}`;
  }
  return `${month} ${day}, ${year}`;
}

function setText(id, v) {
  const el = document.getElementById(id);
  if (el) el.textContent = v;
}

function updateHdrDateFromSummary() {
  if (!summaryData?.asOf) return;
  setText('hdr-date', formatFactsheetDate(summaryData.asOf, currentLang));
}

function setKeyFiguresFromSummary() {
  if (!summaryData) return;
  const pct = (x) => {
    if (typeof x !== 'number' || !isFinite(x)) return '—';
    const val = x * 100;
    const sign = val > 0 ? '+' : '';
    return `${sign}${val.toFixed(1)}%`;
  };
  const num = (x, d=2) => (typeof x === 'number' && isFinite(x)) ? x.toFixed(d) : '—';

  setText('kf-return', pct(summaryData.annualisedReturn));
  setText('kf-maxdd', pct(summaryData.maxDrawdown));
  setText('kf-vol', pct(summaryData.volatility));
  setText('kf-ratios', `${num(summaryData.sharpe, 2)} / ${num(summaryData.sortino, 2)}`);

  const setAria = (id, label) => {
    const el = document.getElementById(id);
    if (el) el.setAttribute('aria-label', label);
  };
  setAria('kf-return', `${T[currentLang].lblKfReturn}: ${pct(summaryData.annualisedReturn)}`);
  setAria('kf-maxdd', `${T[currentLang].lblKfMaxdd}: ${pct(summaryData.maxDrawdown)}`);
  setAria('kf-vol', `${T[currentLang].lblKfVol}: ${pct(summaryData.volatility)}`);
  setAria('kf-ratios', `${T[currentLang].lblKfRatios}: ${num(summaryData.sharpe,2)} / ${num(summaryData.sortino,2)}`);
}

function updateJsonLdDateModifiedFromSummary() {
  if (!summaryData?.asOf) return;
  const iso = summaryData.asOf.toISOString().slice(0, 10);
  const scripts = Array.from(document.querySelectorAll('script[type="application/ld+json"]'));
  for (const s of scripts) {
    try {
      const obj = JSON.parse(s.textContent);
      if (obj && Array.isArray(obj['@graph'])) {
        for (const node of obj['@graph']) {
          if (node && node['@type'] === 'WebPage' && typeof node.dateModified === 'string') {
            node.dateModified = iso;
          }
        }
        s.textContent = JSON.stringify(obj, null, 2);
      }
    } catch(e) {}
  }
}

function buildMonthlyTable() {
  if (!monthlyReturns) return;
  const body = document.getElementById('perf-body');
  if (!body) return;

  const years = Array.from(monthlyReturns.keys()).sort((a,b)=>a-b);
  const fmt = (x) => (typeof x === 'number' && isFinite(x)) ? x.toFixed(1) : '—';
  const classFor = (x) => (typeof x === 'number' && isFinite(x)) ? (x >= 0 ? 'pos' : 'neg') : '';
  const ytdCompoundPct = (vals) => {
    let acc = 1.0;
    let seen = false;
    for (const v of vals) {
      if (typeof v !== 'number' || !isFinite(v)) continue;
      seen = true;
      acc *= (1 + v/100);
    }
    return seen ? (acc - 1) * 100 : null;
  };

  body.innerHTML = '';
  for (const y of years) {
    const tr = document.createElement('tr');
    const th = document.createElement('th');
    th.setAttribute('scope', 'row');
    th.textContent = String(y);
    tr.appendChild(th);

    const vals = [];
    for (let m=1;m<=12;m++) {
      const v = monthlyReturns.get(y)?.get(m);
      vals.push(v);
      const td = document.createElement('td');
      if (typeof v === 'number' && isFinite(v)) td.className = classFor(v);
      td.textContent = fmt(v);
      tr.appendChild(td);
    }

    const ytd = ytdCompoundPct(vals);
    const tdYtd = document.createElement('td');
    tdYtd.className = 'ytd';
    tdYtd.textContent = (typeof ytd === 'number' && isFinite(ytd)) ? ytd.toFixed(1) : '—';
    tr.appendChild(tdYtd);

    body.appendChild(tr);
  }
}

function applyLang(lang) {
  const t = T[lang];
  document.documentElement.lang = t.htmlLang;
  const metaDesc = document.querySelector('meta[name="description"]');
  if (metaDesc) {
    metaDesc.content = lang === 'fr'
      ? 'CryptoVision est un fonds d\'investissement alternatif crypto réglementé (FIA Estonie) géré par SparkCore Fund Management. Stratégie défensive et systématique, liquidité trimestrielle, minimum 50 000 €. Supervisé par la Finantsinspektsioon.'
      : 'CryptoVision is a regulated crypto-asset alternative investment fund (AIF Estonia) managed by SparkCore Fund Management. Defensive & systematic strategy, quarterly liquidity, 50 000 € minimum. Supervised by Finantsinspektsioon.';
  }
  document.title = lang === 'fr'
    ? 'CryptoVision – Factsheet Fonds Crypto | SparkCore Fund Management'
    : 'CryptoVision – Crypto Fund Factsheet | SparkCore Fund Management';

  const s = (id, v) => { const el = document.getElementById(id); if(el) el.textContent = v; };
  const h = (id, v) => { const el = document.getElementById(id); if(el) el.innerHTML = v; };

  s('hdr-title', t.hdrTitle); s('hdr-date', t.hdrDate); s('hdr-cta', t.hdrCta);

  const toCamel = k => k.replace(/-([a-z])/g, (_, c) => c.toUpperCase());
  document.querySelectorAll('[data-seo-id]').forEach(el => {
    const key = toCamel(el.getAttribute('data-seo-id'));
    if (t[key]) el.textContent = t[key];
  });

  s('lbl-launch', t.lblLaunch); s('lbl-strategy', t.lblStrategy);
  s('val-strategy', t.valStrategy); s('lbl-structure', t.lblStructure); s('val-structure', t.valStructure);
  s('lbl-currency', t.lblCurrency); s('lbl-mininvest', t.lblMinInvest); s('lbl-currencies', t.lblCurrencies);
  s('lbl-leverage', t.lblLeverage); s('val-leverage', t.valLeverage); s('lbl-liquidity', t.lblLiquidity);
  s('val-liquidity', t.valLiquidity); s('lbl-lockup', t.lblLockup); s('val-lockup', t.valLockup);
  s('lbl-manager', t.lblManager); s('lbl-brokers', t.lblBrokers); s('lbl-legal', t.lblLegal);
  s('lbl-accounting', t.lblAccounting); s('lbl-compliance', t.lblCompliance);
  s('lbl-fee-entry', t.lblFeeEntry); s('val-fee-entry', t.valFeeEntry);
  s('lbl-fee-mgmt', t.lblFeeMgmt); s('val-fee-mgmt', t.valFeeMgmt);
  s('lbl-fee-perf', t.lblFeePerf); s('val-fee-perf', t.valFeePerf);
  s('lbl-fee-exit', t.lblFeeExit); s('val-fee-exit', t.valFeeExit);
  s('lbl-kf-return', t.lblKfReturn); s('lbl-kf-maxdd', t.lblKfMaxdd);
  s('lbl-kf-vol', t.lblKfVol); s('lbl-kf-ratios', t.lblKfRatios);
  s('val-kf-note', t.valKfNote);
  s('lbl-role-1', t.lblRole1); s('val-bio-1', t.valBio1);
  s('lbl-role-2', t.lblRole2); s('val-bio-2', t.valBio2);
  s('lbl-role-3', t.lblRole3);
  h('val-strat-intro', t.valStratIntro);
  s('lbl-defensive-tag', t.lblDefensiveTag); s('lbl-defensive-name', t.lblDefensiveName);
  s('val-defensive-desc', t.valDefensiveDesc);
  s('lbl-offensive-tag', t.lblOffensiveTag); s('lbl-offensive-name', t.lblOffensiveName);
  s('val-offensive-desc', t.valOffensiveDesc);
  h('val-strat-balance', t.valStratBalance); h('val-strat-risk', t.valStratRisk);
  s('val-chart-note', t.valChartNote);
  s('th-year', t.thYear);
  s('th-jan', t.thJan); s('th-feb', t.thFeb); s('th-mar', t.thMar); s('th-apr', t.thApr); s('th-may', t.thMay); s('th-jun', t.thJun);
  s('th-jul', t.thJul); s('th-aug', t.thAug); s('th-sep', t.thSep); s('th-oct', t.thOct); s('th-nov', t.thNov); s('th-dec', t.thDec);
  s('th-ytd', t.thYtd);
  s('val-perf-note', t.valPerfNote); h('val-disclaimer', t.valDisclaimer);
  s('ftr-company', t.ftrCompany); s('ftr-address', t.ftrAddress); s('ftr-privacy', t.ftrPrivacy);
  document.getElementById('btn-en').classList.toggle('active', lang==='en');
  document.getElementById('btn-en').setAttribute('aria-pressed', lang==='en');
  document.getElementById('btn-fr').classList.toggle('active', lang==='fr');
  document.getElementById('btn-fr').setAttribute('aria-pressed', lang==='fr');

  // Valeurs dynamiques dépendant du format de langue
  updateHdrDateFromSummary();
  setKeyFiguresFromSummary();
}

let currentLang = 'en';
function setLang(lang) {
  currentLang = lang;
  try { localStorage.setItem('sc_factsheet_lang', lang); } catch(e){}
  applyLang(lang);
}

(function initLang() {
  let saved = null;
  try { saved = localStorage.getItem('sc_factsheet_lang'); } catch(e){}
  const browser = (navigator.language || navigator.userLanguage || '').toLowerCase();
  const detected = saved || (browser.startsWith('fr') ? 'fr' : 'en');
  currentLang = detected;
  applyLang(detected);
})();

/* ── SUMMARY + MONTHLY RETURNS (dynamic blocks) ── */
async function fetchFactsheetSummary() {
  const data = await fetchGvizJson(GID_FACTSHEET_SUMMARY);
  const cols = (data.table.cols || []).map(c => c.label);
  const idx = Object.fromEntries(cols.map((c,i)=>[c,i]));
  const rows = data.table.rows || [];
  const row = rows.find(r => (r.c || []).some(c => c && c.v !== null && c.v !== ''));
  if (!row) return;
  const c = row.c || [];
  const v = (name) => c[idx[name]] ? c[idx[name]].v : null;

  const asOf = parseGvizDate(v('as_of'));
  const annualisedReturn = Number(v('annualised_return_pct'));
  const maxDrawdown = Number(v('max_drawdown_pct'));
  const volatility = Number(v('volatility_pct'));

  const sharpeRaw = v('sharpe');
  const sortinoRaw = v('sortino');
  const sharpe = typeof sharpeRaw === 'number' ? sharpeRaw : Number(String(sharpeRaw ?? '').replace(',','.'));
  const sortino = typeof sortinoRaw === 'number' ? sortinoRaw : Number(String(sortinoRaw ?? '').replace(',','.'));

  summaryData = {
    asOf,
    annualisedReturn: isFinite(annualisedReturn) ? annualisedReturn : null,
    maxDrawdown: isFinite(maxDrawdown) ? maxDrawdown : null,
    volatility: isFinite(volatility) ? volatility : null,
    sharpe: isFinite(sharpe) ? sharpe : null,
    sortino: isFinite(sortino) ? sortino : null,
  };

  updateHdrDateFromSummary();
  setKeyFiguresFromSummary();
  updateJsonLdDateModifiedFromSummary();
}

async function fetchMonthlyReturns() {
  const data = await fetchGvizJson(GID_MONTHLY_RETURNS);
  const cols = (data.table.cols || []).map(c => c.label);
  const idx = Object.fromEntries(cols.map((c,i)=>[c,i]));
  monthlyReturns = new Map();

  for (const r of (data.table.rows || [])) {
    const c = r.c || [];
    const year = c[idx.year]?.v;
    const month = c[idx.month]?.v;
    const ret = c[idx.return_pct]?.v;
    if (year == null || month == null || ret == null) continue;

    const y = Math.trunc(Number(year));
    const m = Math.trunc(Number(month));
    const v = Number(ret);
    if (!isFinite(y) || !isFinite(m) || !isFinite(v)) continue;
    if (m < 1 || m > 12) continue;

    if (!monthlyReturns.has(y)) monthlyReturns.set(y, new Map());
    monthlyReturns.get(y).set(m, v);
  }

  buildMonthlyTable();
}

/* SUMMARY + MONTHLY + CHART are now loaded together from R2 by loadFactsheet()
   (see the CHART section). The gviz helpers above are kept but unused. */

/* ── CHART — CryptoVision vs CCi30 (chart_base100) ── */
// Utilise le même fichier de factsheet que le reste (FACTSHEET_SHEET_ID)
const SHEET_ID = FACTSHEET_SHEET_ID;
const SHEET_GID = GID_CHART_BASE100;
const period = [], cryptoVisionValue = [], cci30Base100 = [];

// Unified loader: one fetch of the SparkCore-published JSON (R2, via the Pages
// Function /data/funds/…) replaces the 3 gviz tabs. Units match the previous
// sheet (summary fractions, monthly percent, chart base 100) so render is unchanged.
async function loadFactsheet() {
  try {
    const r = await fetch('/data/funds/cryptovision.json', { cache: 'no-cache' });
    if (!r.ok) throw new Error(`HTTP ${r.status}`);
    const d = await r.json();

    summaryData = {
      asOf: d.as_of ? new Date(d.as_of + 'T00:00:00') : null,
      annualisedReturn: d.summary?.annualised_return ?? null,
      maxDrawdown: d.summary?.max_drawdown ?? null,
      volatility: d.summary?.volatility ?? null,
      sharpe: d.summary?.sharpe ?? null,
      sortino: d.summary?.sortino ?? null,
    };
    updateHdrDateFromSummary();
    setKeyFiguresFromSummary();
    updateJsonLdDateModifiedFromSummary();

    monthlyReturns = new Map();
    for (const m of (d.monthly_returns || [])) {
      if (!monthlyReturns.has(m.year)) monthlyReturns.set(m.year, new Map());
      monthlyReturns.get(m.year).set(m.month, m.return);
    }
    buildMonthlyTable();

    const locale = currentLang === 'fr' ? 'fr-FR' : 'en-US';
    period.length = 0; cryptoVisionValue.length = 0; cci30Base100.length = 0;
    for (const p of (d.chart_base100 || [])) {
      period.push(new Date(p.date + 'T00:00:00').toLocaleDateString(locale, { year: 'numeric', month: 'long' }));
      cryptoVisionValue.push(p.fund);
      cci30Base100.push(p.benchmark);
    }
    renderChart();
  } catch (e) {
    console.error(e);
    const c = document.getElementById('data-container');
    if (c) c.textContent = currentLang === 'fr' ? 'Impossible de charger les données.' : 'Unable to load data.';
  }
}

function renderChart() {
  const draw = () => new ApexCharts(document.querySelector('#chart-el'), {
    series: [
      {name:'CryptoVision', data:cryptoVisionValue},
      {name:'CCi30', data:cci30Base100}
    ],
    chart: {
      height:260, type:'line', zoom:{enabled:false}, toolbar:{show:false},
      animations:{enabled:false}, background:'transparent', fontFamily:'Inter, sans-serif'
    },
    stroke: {width:[3,1.5], dashArray:[0,6]},
    colors: ['#1B2D4D','#988F85'],
    xaxis: {
      categories:period, tickAmount:7, axisTicks:{show:false}, axisBorder:{show:false},
      labels:{hideOverlappingLabels:true, showDuplicates:false, rotate:0, style:{colors:'#5C4E3E', fontFamily:'Inter, sans-serif', fontSize:'9px'}}
    },
    yaxis: {
      logarithmic:false,
      labels:{show:false}, axisTicks:{show:false}, axisBorder:{show:false}
    },
    grid: {yaxis:{lines:{show:false}}, padding:{left:30, right:14, top:-10, bottom:0}},
    legend: {show:false},
    tooltip: {theme:'light', style:{fontFamily:'Inter, sans-serif', fontSize:'11px'}},
  }).render();
  if (document.fonts && document.fonts.ready) { document.fonts.ready.then(draw); } else { draw(); }
}

loadFactsheet();

/* ── Cal.eu Popup Embed ── */
(function (C, A, L) {
  let p = function (a, ar) { a.q.push(ar); };
  let d = C.document;
  C.Cal = C.Cal || function () {
    let cal = C.Cal;
    let ar = arguments;
    if (!cal.loaded) {
      cal.ns = {};
      cal.q = cal.q || [];
      d.head.appendChild(d.createElement("script")).src = A + "/embed/embed.js";
      cal.loaded = true;
    }
    if (ar[0] === L) {
      const api = function () { p(api, arguments); };
      const namespace = ar[1];
      api.q = api.q || [];
      if (typeof namespace === "string") {
        cal.ns[namespace] = cal.ns[namespace] || api;
        p(cal.ns[namespace], ar);
        p(cal, ["initNamespace", namespace]);
      } else p(cal, ar);
      return;
    }
    p(cal, ar);
  };
})(window, "https://app.cal.eu", "init");

Cal("init", { origin: "https://app.cal.eu" });
Cal("ui", {
  theme: "light",
  cssVarsPerTheme: {
    light: { "cal-brand": "#0E1117" }
  }
});
